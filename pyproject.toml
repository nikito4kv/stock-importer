[tool.ruff]
line-length = 88
target-version = "py313"

[tool.ruff.lint]
select = ["E", "F", "I", "B"]
ignore = ["E501"]

[tool.ruff.lint.isort]
known-first-party = [
    "app",
    "browser",
    "config",
    "domain",
    "pipeline",
    "providers",
    "services",
    "storage",
    "ui",
]
